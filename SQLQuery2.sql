use EmployeDB;

create table res(
restname varchar(max),
restloc varchar(max),
opentime time,
closetime time);

alter table res add ID int 

create table menu(
items varchar(max),
rate int,
primary key (ID),
ID int )

drop table men